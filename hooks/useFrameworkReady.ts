import { useEffect } from 'react';

export function useFrameworkReady() {
  useEffect(() => {
    // Framework ready hook for React Native
    // This can be used for any initialization needed when the app starts
  }, []);
}
